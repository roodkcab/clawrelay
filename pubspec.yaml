name: clawrelay
description: "ClawRelay - Chat client for Claude Code API relay"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  drift: ^2.22.1
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.0
  path: ^1.9.0
  http: ^1.2.0
  flutter_markdown: ^0.7.4+3
  markdown: ^7.3.0
  shared_preferences: ^2.3.0
  desktop_drop: ^0.4.1
  file_picker: ^8.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  drift_dev: ^2.22.1
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
